# dengjen-tts-kokoro

Kokoro neural TTS model backend for the dengjen-tts speech synthesis engine.

Loads a Kokoro voice from a config file and runs inference via `ort`, handling vocabulary and voice-style lookup internally.
