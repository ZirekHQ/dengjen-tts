#![forbid(unsafe_code)]

mod dictionary;
mod g2pw;
mod syllable;
mod tokenize;

// Public API lands once every stage is wired up (see the pinyin-phonemizer
// implementation plan for the full task sequence).
