[package]
name = "audio-ops"
version.workspace = true
edition = "2021"
license.workspace = true
description = "Audio post-processing primitives shared across dengjen-tts's speech synthesis pipeline: sample buffers, Hann windowing, and WAV encoding"
repository.workspace = true
homepage.workspace = true

[lints]
workspace = true

[dependencies]
once_cell = "1.18.0"
riff-wave = "0.1.3"

[dev-dependencies]
divan = "0.1.2"
proptest = "1"

[[bench]]
name = "benchmarks"
harness = false
