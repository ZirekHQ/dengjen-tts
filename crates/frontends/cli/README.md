# dengjen-tts-cli

Command-line interface for the dengjen-tts speech synthesis engine.

Builds the `dengjen` binary, which reads text from a file or stdin, synthesizes it with a chosen model config in Lazy, Parallel, or Realtime mode, and writes the resulting audio to a file or stdout.
