name: Publish Java bindings

on:
  # No `environment:` gate on the `release` job below: for workflow_call, merging the version-bump
  # PR is the real decision point (#201) -- but a manual workflow_dispatch bypasses that and
  # publishes whatever tag is given, unreviewed.
  workflow_dispatch:
    inputs:
      tag:
        description: "java-vX.Y.Z to publish"
        required: true
        type: string
      previous_tag:
        description: "Override changelog baseline tag"
        required: false
        type: string
      dry_run:
        description: "Dry Run: publish nothing"
        required: false
        type: boolean
        default: false
  workflow_call:
    inputs:
      tag:
        description: "java-vX.Y.Z to publish"
        required: true
        type: string
      previous_tag:
        description: "Override changelog baseline tag"
        required: false
        type: string
      dry_run:
        required: false
        type: boolean
        default: false
    secrets:
      JRELEASER_MAVENCENTRAL_USERNAME:
        required: true
      JRELEASER_MAVENCENTRAL_PASSWORD:
        required: true
      JRELEASER_GPG_PASSPHRASE:
        required: true
      JRELEASER_GPG_SECRET_KEY:
        required: true
      JRELEASER_GPG_PUBLIC_KEY:
        required: true

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    outputs:
      skip: ${{ steps.check.outputs.skip }}
    steps:
      # inputs.tag (workflow_dispatch) is free text from whoever has dispatch
      # access -- unvalidated it'd publish whatever ref it names, real GPG/OSSRH secrets included.
      - name: Validate release tag input
        if: github.event_name == 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ inputs.tag }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          if ! echo "${RELEASE_TAG}" | grep -qE '^java-v[0-9]+\.[0-9]+\.[0-9]+.*$'; then
            echo "::error::'${RELEASE_TAG}' doesn't look like a java-vX.Y.Z tag"
            exit 1
          fi
          if ! gh api "repos/${REPO}/git/refs/tags/${RELEASE_TAG}" >/dev/null 2>&1; then
            echo "::error::'${RELEASE_TAG}' is not an existing tag on this repository -- refusing to build from an arbitrary ref"
            exit 1
          fi

      - name: Check whether this version is already on Maven Central
        id: check
        env:
          RELEASE_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          version="${RELEASE_TAG#java-v}"
          url="https://repo1.maven.org/maven2/io/github/zirekhq/dengjen-tts/${version}/dengjen-tts-${version}.pom"
          status="$(curl -s -o /dev/null -w '%{http_code}' "$url")"
          case "$status" in
            200)
              echo "::notice::dengjen-tts ${version} is already on Maven Central -- skipping"
              echo "skip=true" >> "$GITHUB_OUTPUT"
              ;;
            404)
              echo "skip=false" >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "::error::unexpected HTTP ${status} checking Maven Central for dengjen-tts ${version}" >&2
              exit 1
              ;;
          esac

  build-natives:
    needs: check
    if: needs.check.outputs.skip != 'true'
    runs-on: ${{ matrix.platform.runner }}
    timeout-minutes: 30
    permissions:
      contents: read
    strategy:
      matrix:
        platform:
          - classifier: linux-x86_64
            runner: ubuntu-22.04
          - classifier: linux-aarch64
            runner: ubuntu-22.04-arm
          - classifier: windows-x64
            runner: windows-2022
          - classifier: macos-aarch64
            runner: macos-latest
    steps:
      - uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4.3.0
        with:
          # A workflow_dispatch run whose `tag` input doesn't match the dispatched ref would
          # otherwise build from the wrong commit and publish it under the given tag's label.
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.sha }}
          persist-credentials: false
      - uses: ./.github/actions/setup-native-build-deps # zizmor: ignore[self-repository] CI is pinned to zizmor v1.28.0, which predates $/ syntax support
        timeout-minutes: 15
        with:
          # Windows/macOS have no apt-get; their compiler toolchain installs below stay inline.
          apt-packages: ${{ runner.os == 'Linux' && 'llvm llvm-dev llvm-runtime libclang-dev' || '' }}
      - name: Set CMAKE_GENERATOR for Windows
        if: runner.os == 'Windows'
        shell: bash
        run: echo "CMAKE_GENERATOR=Ninja" >> "$GITHUB_ENV"
      - name: install libclang (windows)
        if: runner.os == 'Windows'
        run: choco install llvm
      - if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1.13.0
        with:
          arch: x64
      - name: install libclang (macos)
        if: runner.os == 'macOS'
        run: |
          brew install llvm
          echo "LIBCLANG_PATH=$(brew --prefix llvm)/lib" >> "$GITHUB_ENV"
      # channel comes from rust-toolchain.toml
      - uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87 # stable
      - name: Build libdengjen
        run: cargo build --release --manifest-path Cargo.toml -p libdengjen --locked
      - name: Stage native library
        shell: bash
        run: |
          mkdir -p "native-artifacts/${{ matrix.platform.classifier }}"
          case "${{ runner.os }}" in
            Linux) lib="target/release/liblibdengjen.so" ;;
            macOS) lib="target/release/liblibdengjen.dylib" ;;
            Windows) lib="target/release/libdengjen.dll" ;;
          esac
          cp "$lib" "native-artifacts/${{ matrix.platform.classifier }}/"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: native-${{ matrix.platform.classifier }}
          path: native-artifacts/${{ matrix.platform.classifier }}

  # Only gates workflow_dispatch: the release.yml chain's real decision point is merging the
  # version-bump PR (#201 removed release.yml's own post-tag gate for the same reason this one
  # would be too late there -- the tag is already public by then).
  gate:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment: release
    steps:
      - run: echo "approved"

  release:
    needs: [check, build-natives, gate]
    if: >-
      always() && needs.check.outputs.skip != 'true' &&
      (needs.gate.result == 'success' || needs.gate.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4.3.0
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.sha }}
          fetch-depth: 0 # needed for JReleaser's conventional-commits changelog; VERSIONING_GIT_TAG (below) drives the actual version resolution
          persist-credentials: false
      - uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c # v6.0.0
        with:
          java-version: 25
          distribution: temurin
      - uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0
        with:
          # A publish-credentialed job must never consume a cache entry a
          # less-trusted run could have written.
          cache-disabled: true
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          path: bindings/java/native-artifacts
          pattern: "native-*"
      - name: Flatten downloaded native artifacts
        working-directory: bindings/java/native-artifacts
        run: for d in native-*; do mv "$d" "${d#native-}"; done
      - name: Publish
        working-directory: bindings/java
        env:
          # git-versioning reads GITHUB_REF (not the checkout), so naming the tag here is what
          # makes this build the release version instead of a branch snapshot.
          VERSIONING_GIT_TAG: ${{ inputs.tag }}
          JRELEASER_PREVIOUS_TAG_OVERRIDE: ${{ inputs.previous_tag }}
          JRELEASER_DRY_RUN_OVERRIDE: ${{ inputs.dry_run }}
          JRELEASER_MAVENCENTRAL_USERNAME: ${{ secrets.JRELEASER_MAVENCENTRAL_USERNAME }}
          JRELEASER_MAVENCENTRAL_PASSWORD: ${{ secrets.JRELEASER_MAVENCENTRAL_PASSWORD }}
          JRELEASER_GPG_PASSPHRASE: ${{ secrets.JRELEASER_GPG_PASSPHRASE }}
          JRELEASER_GPG_SECRET_KEY: ${{ secrets.JRELEASER_GPG_SECRET_KEY }}
          JRELEASER_GPG_PUBLIC_KEY: ${{ secrets.JRELEASER_GPG_PUBLIC_KEY }}
          JRELEASER_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew publish jreleaserFullRelease --no-daemon --stacktrace
      - if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: jreleaser-release
          path: |
            bindings/java/build/jreleaser/trace.log
            bindings/java/build/jreleaser/output.properties
          if-no-files-found: ignore
